# Paginated Table
This component provides a generic table with paginated results. Rows can be sorted and
filtered by any of the fields of the rows.

This is handy reusable component for a variety of projects the require displaying an array of objects in a table.
